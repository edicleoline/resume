import { styled } from '@mui/material/styles';

export const Aside = styled('aside')({
    position: 'sticky',
    top: '80px',
    maxHeight: 'calc(100vh - 148px)',
    overflow: 'auto',
    marginLeft: 'auto',
    height: '100%'
});
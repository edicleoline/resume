import { styled } from '@mui/material/styles';
import MenuItem from '@mui/material/MenuItem';

export const ActionSheetMenuItem = styled(MenuItem)(({ theme }) => ({
    borderBottom: 'solid 1px #ddd'
}));